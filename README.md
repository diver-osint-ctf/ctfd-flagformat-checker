# CTFd Flag Format Checker

Validates flag submissions against regex patterns in CTFd.

## Install

Copy to CTFd's plugins directory and restart.

## Usage

1. Go to `/admin/flag-format`
2. Set regex pattern (e.g., `flag\{.*\}`)
3. Test with sample flags
4. Save settings
